import {
  createBaseVNode,
  createElementBlock,
  defineComponent,
  normalizeClass,
  openBlock,
  ref,
  renderSlot,
  toDisplayString
} from "./chunk-ZY5X6FX7.js";
import "./chunk-BUSYA2B4.js";

// node_modules/vue-writer/dist/vue-writer.js
var E = { class: "is-typed" };
var I = { class: "typed" };
var W = defineComponent({
  __name: "vue-writer",
  props: {
    array: {},
    eraseSpeed: { default: 100 },
    typeSpeed: { default: 200 },
    delay: { default: 2e3 },
    intervals: { default: 500 },
    start: { default: 0 },
    caret: { default: "cursor" },
    iterations: { default: 0 }
  },
  emits: ["typed"],
  setup(n, { emit: f }) {
    const {
      array: a,
      eraseSpeed: p,
      typeSpeed: i,
      delay: m,
      intervals: y,
      start: _,
      caret: h,
      iterations: S
    } = n, g = f, r = ref(""), c = ref(0), e = ref(false), t = ref(0), l = ref(0);
    function o() {
      let s = 0;
      if (l.value < a[t.value].length)
        e.value || (e.value = true), r.value += a[t.value].charAt(l.value), l.value += 1, setTimeout(o, i);
      else {
        if (c.value += 1, T(a[t.value]), c.value === a.length && (s += 1, s === S))
          return e.value = false;
        e.value = false, setTimeout(v, m);
      }
    }
    function v() {
      l.value > 0 ? (e.value || (e.value = true), r.value = a[t.value].substring(0, l.value - 1), l.value -= 1, setTimeout(v, p)) : (e.value = false, t.value += 1, t.value >= a.length && (t.value = 0), setTimeout(o, i + y));
    }
    function T(s) {
      g("typed", s);
    }
    return setTimeout(o, _), (s, b) => (openBlock(), createElementBlock("div", E, [
      renderSlot(s.$slots, "default"),
      createBaseVNode("span", I, toDisplayString(r.value), 1),
      createBaseVNode("span", {
        class: normalizeClass(h + " " + { typing: e.value })
      }, " ", 2)
    ]));
  }
});
var A = {
  install: (n) => {
    n.component("VueWriter", W);
  }
};
export {
  W as VueWriter,
  A as default
};
//# sourceMappingURL=vue-writer.js.map
